package testcase;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

import ExcelConfig.ExcelReaderUtil;


public class test {

	public static void main(String[] args) throws Exception {
		
		
		/*DataPreparation data = new DataPreparation();
		data.prepareData("45566");
		System.out.println(data.data("MPC_PERC"));*/
		
		
		/*ExcelReaderUtil obj  = new ExcelReaderUtil("abc.xlsx");
		String s = obj.getCellData("Sheet1", "String", 2);
		System.out.println(s);
		System.out.println(s.equals("abc def (terms and conditions apply)"));
		
		
		for(int i=0; i<s.length();i++)
		{
		if(s.contains("</a>"))
		{
			 s= s.substring(0,s.indexOf("<a"))+s.substring(s.indexOf(">")+1);
			 s=s.substring(0, s.indexOf("<"))+s.substring(s.indexOf(">")+1);
			
		}
		}
		System.out.println(s.indexOf("<a"));
		System.out.println(s.lastIndexOf("a>"));
		s=s.substring(0, 9)+s.substring(74);
		System.out.println(s);*/
		
		
		String s="abc";
		

		System.out.println(s.substring(0, 2));
		
		for (int i=0; i<s.length(); i++)
		{
			
			
			
			
			
		}
		
				
		
		
	}

}
